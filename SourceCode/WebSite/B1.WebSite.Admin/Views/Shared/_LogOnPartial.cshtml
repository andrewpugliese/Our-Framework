@if(Request.IsAuthenticated) {
    <text>Welcome <b>@Context.User.Identity.Name</b>!
    [ @Html.ActionLink("Sign Off", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Sign On", "LogOn", "Account") ]
}
